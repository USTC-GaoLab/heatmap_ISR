# Environment record

Record the R and Bioconductor versions and versions of edgeR, AnnotationDbi,
organism annotation packages, pheatmap, ggplot2 and other dependencies used by
the ISR workflow. Record input provenance separately without committing raw data.
